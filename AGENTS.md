# RemoteCodex 작업 지침

이 지침은 저장소 전체에 적용한다. 사용자의 현재 명시적 지시를 우선한다.

## 역할 분담

- **코딩의 기본 담당은 MiniMax M3**다. 기능 구현, 버그 수정, 리팩터링,
  테스트 코드 작성 등 구현 작업을 먼저 M3에 배정한다.
- 주 에이전트는 요구사항 해석, 설계, 작업 분해, 모델 배정, 검토, 통합,
  최종 검증을 책임진다. M3의 완료 보고만으로 작업 완료를 선언하지 않는다.
- 설계·분석·검토·리뷰는 아래 기준으로 GPT 모델에 배정한다. 이는 이 프로젝트의
  운영 기준이며 모델 가격이나 성능에 대한 보장으로 해석하지 않는다.

| 작업 난이도와 범위 | 기본 담당 |
| --- | --- |
| 단순한 문서·요구사항 정리, 작은 변경의 국소 검토 | GPT-5.6 Luna |
| 일반적인 구현 설계, 코드 흐름 분석, 버그 원인 분석, 모듈 단위 리뷰 | GPT-5.6 Terra |
| 여러 모듈에 걸친 설계·통합 검토, 복잡한 회귀 분석과 기술적 선택 | GPT-5.6 Sol |
| 큰 아키텍처 결정, 인증·권한·동시성·세션 수명 등 위험도 높은 변경, 해결되지 않은 난제 | GPT-6 Astra |

- 난이도뿐 아니라 변경의 영향, 불확실성, 실패 비용을 고려해 자율적으로 배정한다.
  위험도가 높은 변경은 분량이 작아도 상위 검토 모델을 사용한다.
- 모든 작업을 모든 모델에 중복 배정하지 않는다. 독립적인 작업이나 별도 검토가
  실제로 도움이 될 때만 하위 에이전트 또는 병렬 작업을 사용한다.
- 호출할 때는 현재 도구가 제공하는 실제 모델 식별자를 확인한다. 지원되지 않는
  모델을 사용했다고 주장하거나 다른 모델을 그 이름으로 표시하지 않는다.

## M3 실행과 로컬 API 키

- 사용자는 M3 API 키가 로컬에 있다고 명시했다. 기존 환경 변수, 로컬 설정,
  실행 도구를 확인하여 재사용한다. 키를 확인하기 위해 값을 출력하지 않는다.
- API 키·토큰·인증 헤더를 소스, 문서, 로그, 커밋, 공개 저장소에 기록하지 않는다.
  하위 에이전트의 프롬프트에도 키를 붙여 넣지 않는다.
- 처음 사용할 때 최소 요청으로 실제 M3 호출 가능 여부를 확인한 다음 작업을 맡긴다.
  키나 실행 경로를 찾지 못하면 그 상태를 정확히 보고하고 아래 대체 정책을 적용한다.
- M3에는 대상 파일, 필요한 문맥, 제약, 완료 조건을 포함한 작고 명확한 작업을 준다.
  Windows에서 길거나 한글이 포함된 프롬프트는 가능한 한 표준 입력으로 전달한다.
- **M3에는 한 번에 한 가지 수정 목표만 맡긴다.** 기본 단위는 관련 파일 1~3개와
  그 변경을 확인하는 검사 하나다. 전체 단계나 여러 기능을 한꺼번에 구현시키지 않는다.
- 큰 작업은 인터페이스와 선행 관계를 먼저 정리한 뒤 순서대로 쪼갠다. 각 단위의
  패치를 검토·검증한 후 다음 단위를 전달하고, 실패하면 해당 단위만 좁혀 재시도한다.
- 지시 전에 주 에이전트가 실제 오류, 대상 코드, 설치된 API를 확인한다. 프롬프트에는
  목표 하나, 정확한 파일·코드 문맥, 바꿀 범위, 유지할 동작, 완료 검사와 출력 형식을 넣는다.
- **M3 호출 실패의 원인은 주 에이전트가 분석하고 다음 지시에 반영한다.** 문맥 부족,
  전송·이스케이프·패치 형식, API 오해, 구현·검증 실패를 구분한다. 도구나 전달 형식의
  실패를 곧바로 코딩 능력 문제로 판단하지 않고, 같은 지시를 그대로 반복하지 않는다.
- 작은 함수나 짧은 파일은 필요 시 코드 원문을 요청해 JSON 중첩 이스케이프를 피한다.
  패치가 필요하면 추가·삭제 표시를 포함한 정확한 형식을 명시한다. 형식 변환 후에도
  허용 파일과 변경 범위를 확인하며, 응답을 검토 없이 실행하거나 적용하지 않는다.
- Windows PowerShell 5.1에서 표준 입력으로 한글을 전달할 때는 `$OutputEncoding`을
  UTF-8로 설정하거나 UTF-8 프롬프트 파일을 읽는다. 전달 과정에서 `?`로 바뀐 문자를
  모델의 구현 오류로 오인하지 않는다. 패치 검증이 실패하면 후속 실행도 즉시 중단한다.
- 재시도에는 이전 실패의 구체적 원인과 수정된 조건만 추가한다. 검증된 전달 방식과
  반복 방지책은 비밀정보를 제외하고 작업 기록에 남겨 다음 작업에 재사용한다.
- 반환된 변경은 실제 코드와 대조하고 필요한 검사를 실행한 후 통합한다.
- 프롬프트의 API 예시는 축약 의사 코드 대신 실제 호출식으로 쓴다. `await pair()`처럼
  수신 객체가 빠진 예시를 주지 않고 `await host.pair()`처럼 그대로 실행 가능한 형태를 준다.
  실패가 주 에이전트의 잘못된 문맥에서 비롯되면 모델 실패와 구분해 기록한다.
- UI 선택자는 현재 Svelte 컴포넌트와 실제 DOM에서 확인한다. 남아 있는 CSS 클래스만 보고
  선택자를 만들지 않는다. 버튼 이름이 반복되면 sidebar/pane 같은 실제 영역으로 범위를 제한한다.
- Windows 계측의 생성 시각은 UTC ticks 문자열로 보존한다. 부동소수점 변환을 거치지 않는다.
  실행 중인 fixture가 쓰는 파일을 읽을 때는 공유 읽기/쓰기 모드를 명시한다.
- 네이티브 배포 UI 검증은 Tauri CLI의 release 빌드로 수행한다. 일반 `cargo build --release`는
  개발 URL을 사용하는 바이너리를 만들 수 있으므로 내장 프런트엔드 검증의 대체 근거로 쓰지 않는다.
- PowerShell 5.1에서 `ErrorActionPreference=Stop`과 `*>`를 조합하면 외부 명령의 stderr 경고가
  예외로 승격될 수 있다. 장시간 빌드 로그는 숨긴 자식 프로세스의 stdout/stderr를 직접
  리디렉션하고 실제 종료 코드와 결과 파일을 확인한다. 이 전달 실패를 빌드·M3 오류와 구분한다.
- M3의 추론을 끈 호출 실패만으로 모델의 구현 능력을 평가하지 않는다. 현재 API는
  `thinking.type=adaptive|disabled`를 허용하고 `enabled`는 400으로 거절했다.
  기계적인 치환에는 disabled를 사용할 수 있지만 로직 수정은 adaptive와 공식 권장
  temperature 1.0 / top_p 0.95를 비교한다. 실제 요청 설정과 검증 결과를 기록한다.
- 추론 출력도 토큰 한도에 포함된다. `length`와 빈 최종 코드가 나오면 전달/한도 실패로
  분류한다. 작업을 줄이거나 한도를 제한적으로 조정하며 무한히 늘리지 않는다.
- 다른 작업에서 발생한 실패를 새 작업의 M3 생략 사유로 삼지 않는다. 새 작업은 다시
  작은 M3 단위로 배정하고 적용/폐기/검증 결과 및 확인 가능한 사용량을 구분한다.
- 코드 생성 호출에는 도구/파일 접근이 없다는 점과 실제 프로젝트 이름을 system 지시로 명시한다.
  코드만 반환하도록 요청하고, 현재 API가 지원하면 낮은 temperature와 reasoning_split을 시험한다.
  이 설정이 정확성을 보장하지 않으므로 이전 방식과 실제 성공률·총 토큰을 비교한다.
- finish_reason이 length인 응답은 미완성으로 취급한다. 설명을 코드로 오인하거나 잘린 파일을
  적용하지 않는다. 길이 부족과 잘못된 API/타입은 원인을 구분해 인계한다.
- M3가 저장소와 무관한 제품명, 존재하지 않는 도구 호출, 자료구조 필드를 만들면 해당 출력을
  적용하지 않는다. 작은 함수의 실제 시그니처·입력 예·설치된 API를 제공해 범위를 줄인다.
- 여러 검토 에이전트가 동시에 M3 실패를 겪으면 Luna 한 명에게 모든 코딩을 대기시키지 않는다.
  설계가 끝난 검토 에이전트의 슬롯을 반환하고, 압축된 인계로 필요한 Luna 구현 작업을 병렬 배치한다.


## 코딩 실패 시 대체 정책

- M3가 반복적으로 코딩에 실패하거나 응답 정지, 유효하지 않은 패치, 동일 오류를
  되풀이하면 무한 재시도하지 않는다. 첫 실패의 원인을 확인하고 수정된 문맥으로
  제한적으로 재시도한다. 기본적으로 같은 문제로 2회 연속 실패하면 전환을 검토한다.
- 전환 시 **GPT-5.3 Codex Spark 또는 GPT-5.6 Luna**에 코딩을 배정한다.
  범위가 작은 작업은 사용 가능한 Spark를 우선 검토하고, 더 많은 문맥이나 통합이
  필요한 작업은 Luna를 검토한다. Spark를 호출할 수 없으면 Luna를 사용한다.
- 모델 선택은 이미 사용한 입력·출력 토큰, 반복 전달할 문맥, 예상 재시도 및 리뷰
  토큰까지 포함한 **작업 전체 토큰 사용량**을 고려한다. 토큰 수를 확인할 수 없으면
  추정임을 밝히고 수치를 지어내지 않는다. 토큰 수와 금액을 동일시하지 않는다.
- 실패한 전체 대화를 반복 전달하지 말고 현재 변경, 실패 원인, 필요한 파일과
  완료 조건으로 인계 내용을 압축한다.
- 이 기준 안에서 재시도·전환·검토 모델 배정은 매번 사용자 확인 없이 진행한다.
  전환한 이유와 실제 사용한 모델은 작업 결과에 간단히 남긴다.
- M3와 지정된 대체 모델을 모두 사용할 수 없다면 사용 가능한 선택지와 제한을
  보고한다. 다른 모델을 지정 모델인 것처럼 사용하지 않는다.

## 검증과 작업 범위

- 작업 전 `README.md`, `IMPLEMENTATION_STATUS.md`, 관련 `SPEC.md` 절을 확인한다.
- 변경에 필요한 검사만 실행하고, 실제 실행 결과와 미검증 항목을 구분한다.
  단위 테스트 통과를 Windows 빌드나 실제 원격 동작 성공으로 대신하지 않는다.
- 이 지침은 모델 배정 정책이다. 제품 기능 확장, 운영 설정 변경, 실제 사용자 파일
  삭제 등을 별도로 허가하는 지침으로 해석하지 않는다.

## 실제 위임 기록

- 주 에이전트가 M3 API 호출과 적용 결과를 직접 관리한다. GPT 하위 에이전트는 기본적으로 설계·읽기 전용 리뷰·문서 담당이며, 지정된 대체 코딩 과제일 때만 구현한다.
- M3 코드의 원형, 통합 중 수정한 API/변수/타입, 폐기 이유를 구분한다. 생성 호출을 했다는 사실만으로 최종 전체 파일을 M3 단독 구현이라고 표시하지 않는다.
- 이번 PC 잔여 작업의 직접 호출·토큰·적용 결과는 `docs/test-results/pc-completion-2026-09-12/m3-calls.json`에 기록한다. 이전 19개 선택 호출은 `docs/test-results/spec-completion-2026-09-12/m3-recent-calls.json`에 남긴다. 두 기록은 전체 세션/계정 비용이 아니며 GPT 토큰은 관측하지 못했다.
